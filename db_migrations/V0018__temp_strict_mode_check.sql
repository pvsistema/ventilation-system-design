-- ВРЕМЕННО: включаем строгий режим, чтобы убедиться, что защита расчётов
-- действительно срабатывает на живых серверах. Сразу после проверки режим
-- возвращается в мягкий (следующая миграция) — включать его по-настоящему
-- можно только когда расчёты без лицензии сойдут на нет.
UPDATE app_settings SET value = 'strict', updated_at = NOW()
WHERE key = 'compute_license_mode';
